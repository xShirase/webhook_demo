{
    "type": "object",
    "title": "Schema of the Collection Request",
    "required": [
        "callbackUrl",
        "provider"
    ],
    "properties": {
        "callbackUrl": {
            "type": "string",
            "title": "The Callback URL",
            "default": "",
            "pattern": "^https?://.+"
        },
        "provider": {
            "type": "string",
            "minLength": 1,
            "title": "The Provider to Collect",
            "default": ""
        }
    }
}